# Архитектура проекта

## Слои FSD (сверху вниз)

```
app/                    # Слой 1: Настройки приложения, роутинг
├── (moduls)/           # Группы страниц по модулям
├── _layout.tsx         # Корневой layout
└── index.tsx           # Стартовая страница

features/               # Слой 2: Бизнес-логика страниц и модулей
├── [module]/           # Логика конкретного модуля
│   └── [page]-model.ts # MobX store для страницы
└── [feature]/          # Глобальные фичи

packages/               # Слой 3: Переиспользуемый код (web + mobile)
├── entities/           # Бизнес-сущности
│   └── [entity]/
│       ├── api.ts      # Запросы к API
│       ├── type.ts     # Типы данных
├── features/           # Общая логика между модулями
├── functions/          # Чистые функции-хелперы
├── shared-components/  # Сложные UI-компоненты (Header, Card и т.д.)
├── shared-ui/          # Примитивные UI-компоненты (Button, Input, Modal)
├── widgets/            # Блоки страниц со своей логикой
└── design-tokens/      # Дизайн-система (цвета, типографика)
```

## Правила импортов

✅ **Можно:**
- entities → shared-ui, shared-components, functions
- features → entities, shared
- widgets → features, entities, shared
- app → все слои

❌ **Нельзя:**
- shared → entities, features (нижний слой не импортирует верхний)
- entities → features (сущность не знает о фичах)


## Работа с существующим кодом

1. **НЕ изменять существующие файлы** — только дополнять новыми
2. **НЕ ломать обратную совместимость** экспортов
3. **НЕ создавать дубликаты** — сначала проверить, есть ли нужный компонент в shared
4. **Если нужно изменить существующий компонент** — создать новый рядом с пометкой V2

## Структура файлов

```
📁 [entity/feature/widget]/
├── index.tsx              # Основной экспорт (если компонент)
├── [name].tsx             # Компонент
├── [name]-model.ts        # MobX store
├── [name]-api.ts          # Запросы
├── types.ts               # Типы (если много)
└── components/            # Локальные компоненты
    └── [subcomponent].tsx
```

## Ключевые директории

```
`src/api/`                      # Инстансы axios, роуты API
`src/app/`                      # Все страницы и роутинг
`packages/entities/`            # Модели данных, типы, API запросы к сущностям
`src/features/`                 # Логика страниц (MobX сторы)
`packages/shared/ui-components` # общие компоненты, как шапка навигационная панель и т.д.
`packages/shared/ui-kit`        # UI-кит (кнопки, инпуты, модалки)
`packages/widgets/`             # Сложные блоки (панели, табы, карточки с логикой)
```

## Типы файлов в app/

```
`(name)`           # Группировка (не влияет на URL)  `(auth)/`
`[name]`           # Динамический параметр `[id]/`
`_layout.tsx`      # Обертка для дочерних страниц `Stack, Tabs`
`index.tsx`        # Главная страница папки
`+not-found.tsx`   # 404 страница
`+middleware.ts`   # Проверка авторизации
```